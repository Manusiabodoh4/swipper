const config = require("../config.json");

module.exports = {
  account   : config.accountBNI,  
  account2  : config.accountMandiri,
  account3  : config.accountPermata,
  account4  : config.accountBRI
}

